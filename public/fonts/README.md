Reserved for self-hosted font files if a future subset/fallback is needed.
Alexandria and Plus Jakarta Sans are currently loaded via `next/font/google`
in `src/app/[locale]/layout.tsx`, not from static files here.
